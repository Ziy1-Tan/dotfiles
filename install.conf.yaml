- defaults:
    link:
      create: true
      relink: true
    shell:
      quiet: true
      stdout: true
      stderr: true
    create:
      mode: 0755

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- create:
    - ~/.ssh
    - ~/.zsh
    - ~/.vim/plugged
    - ~/.config

- link:
    ~/.zshrc: zshrc
    ~/.zprofile: zprofile
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.gitconfig: gitconfig
    ~/.config/:
      glob: true
      path: config/*
      relink: true

- shell:
    - description: Installing system dependencies (zsh, curl)
      command: |
        if [ "$(uname)" = "Darwin" ]; then
          if ! command -v brew >/dev/null 2>&1; then
            /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
          fi
          brew install zsh curl 2>/dev/null || true
        elif [ "$(uname)" = "Linux" ]; then
          if ! command -v zsh >/dev/null 2>&1 || ! command -v curl >/dev/null 2>&1; then
            sudo apt-get update && sudo apt-get install -y zsh curl
          fi
          if ! command -v brew >/dev/null 2>&1; then
            /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install.sh)"
          fi
        fi

    - description: Changing default shell to zsh
      command: |
        if [ "$(basename "$SHELL")" = "zsh" ]; then
          echo "Default shell is already zsh"
          exit 0
        fi
        ZSH_PATH="$(command -v zsh)"
        if [ -z "$ZSH_PATH" ]; then
          echo "zsh not found, skipping shell change"
          exit 0
        fi
        echo "Current shell is $SHELL, switching to zsh..."
        if [ "$(uname)" = "Linux" ]; then
          sudo usermod -s "$ZSH_PATH" "$USER"
        else
          sudo chsh -s "$ZSH_PATH" "$USER"
        fi

    - [git submodule update --init --recursive, Installing submodules]
    - description: Installing zoxide
      command: |
        if ! command -v zoxide >/dev/null 2>&1; then
          curl -sSfL https://raw.githubusercontent.com/ajeetdsouza/zoxide/main/install.sh | sh
        fi

    - description: Installing fzf
      command: |
        if ! command -v fzf >/dev/null 2>&1; then
          if [ -d ~/.fzf ]; then
            (cd ~/.fzf && git pull --quiet && ./install --no-bash --no-fish)
          else
            git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf && (~/.fzf/install --no-bash --no-fish)
          fi
        fi

    - description: Installing vim-plug
      command: |
        if ! command -v vim >/dev/null 2>&1; then
          echo "Vim not found, skipping vim-plug installation"
          exit 0
        fi
        if [ ! -f ~/.vim/autoload/plug.vim ]; then
          mkdir -p ~/.vim/autoload
          curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
        fi

    # fd 被 fzf.zsh 的默认搜索命令引用，必须安装。
    # Linux 用 apt（包名 fd-find，二进制是 fdfind，软链成 fd 放到 ~/.local/bin）；
    # macOS 用 brew。不要在 Linux 上走 Linuxbrew——拉取 ghcr.io 在国内网络会挂起。
    - description: Installing fd (apt on Linux / brew on macOS)
      command: |
        if command -v fd >/dev/null 2>&1; then
          echo "fd already installed"
        elif [ "$(uname)" = "Linux" ]; then
          sudo apt-get install -y fd-find >/dev/null 2>&1 && {
            mkdir -p "$HOME/.local/bin"
            ln -sf "$(command -v fdfind)" "$HOME/.local/bin/fd"
          } || echo "WARN: could not install fd-find (fzf will fall back to find)"
        else
          brew install fd 2>/dev/null || echo "WARN: could not brew install fd"
        fi

    - description: Installing zinit
      command: |
        if [ ! -d ~/.local/share/zinit ]; then
          bash -c "$(curl --fail --show-error --silent --location https://raw.githubusercontent.com/zdharma-continuum/zinit/HEAD/scripts/install.sh)"
        else
          echo "zinit is already installed at ~/.local/share/zinit"
        fi
